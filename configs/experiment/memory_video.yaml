# @package _global_

defaults:
  - override /data: memory 
  - override /model: ddpm_video
  - override /callbacks: default
  - override /trainer: gpu
  #- override /trainer:  ddp 
  - override /logger: wandb
  - override /paths: default
  #- override /debug: overfit

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["ddpm", "debug", "memory_maze"]

seed: 42 
test: False


trainer:
  detect_anomaly: False #True #True
  strategy: ddp_find_unused_parameters_true
  inference_mode: False
  min_epochs: 10
  max_epochs: 1000
  gradient_clip_val: 1 
  val_check_interval: 100 #500 #00 #0 #500 
  

  check_val_every_n_epoch:  #5
  #limit_train_batches: 1
  limit_val_batches: 1 #20
  

  log_every_n_steps: 1
  num_sanity_val_steps: 1
  
  #overfit_batches: 1
  precision: #bf16-mixed #null #fp32 #bf16-mixed
  ###
  ###
  ###
  ###
  ###

#paths:
#  data_dir: /data/cvg/sebastian/memory_maze/memory-maze-9x9 
  
#ckpt_path: logs/train/runs/2025-02-21_22-04-15/checkpoints/last.ckpt  # null

model:
  ckpt_path: #logs/train/runs/2025-03-16_19-48-49/checkpoints/last.ckpt #null #logs/train/runs/2025-02-21_22-04-15/checkpoints/last.ckpt  # null
  lora_finetune: False 
  compile_model:  False #true_without_ddp_optimizer # True
  batch_size: ${data.batch_size}

  data_mean: 0.5
  data_std: 0.5

  # VAE Configuration
  is_latent_diffusion: False
  is_latent_online: False
  latent_downsampling_factor: [1, 1]


  # Metrics Configuration
  metrics: [mse, psnr, ssim] #[fvd, is, fid, lpips, mse, psnr, ssim]
  metrics_batch_size: 16
  n_metrics_frames: #null # 50

  # Checkpoint
  strict_load: False

  # Logging Configuration
  log_grad_norm_freq: 0 #100 #${trainer.log_every_n_steps}
  log_deterministic: False
  log_sanity_generation: True #False
  log_max_num_videos: 32


  # General Configuration
  x_shape: ${data.observation_shape}
  n_frames: ${data.n_frames} # Number of frames to be used for generating videos 
  max_frames: ${data.max_frames} # ... during training
  context_frames: ${data.context_length} # number of context frames

  # Generation Configuration
  sliding_context_len: 1  # if 0 only first iteration has context_frames
  #context_frames_in_autoregressive_prediction: 0 # the amount of context frames to be used in autoregressive prediction (after the first iteration)
  sampling_timesteps: 10
  cat_context_in_c_dim: True
  generate_in_noise_dim: True #True #True

  #use_causal_mask: True #### 
  #clip_noise: 20 
  noise_level: random_uniform # random_independent
  scheduling_matrix: full_sequence
  context_dropout: 0.0
  reconstruction_guidance: 0.0



  use_causal_mask: False #True #False
  clip_noise: 20.0

  # put these into data maybe?
  external_cond_processing: null 
  external_cond_dim: 0
  
  # Tasks
  tasks: [prediction,] #, interpolation]
  diffusion_model:
    _partial_: true
    _target_: src.models.components.diffusion.DiscreteDiffusion #ContinuousDiffusion 
    model: 
      _target_: insert_memory.MemoryDiT3D 
      x_shape: ${data.observation_shape}
      cat_conditioning: ${model.cat_context_in_c_dim}
      max_tokens: ${data.max_frames}
      external_cond_dim: 0
      hidden_size: 192
      patch_size: 4
      variant: full
      pos_emb_type: rope_3d #learned_3d
      depth: 12
      num_heads: 4
      mlp_ratio: 4
      use_gradient_checkpointing: False
      use_fourier_noise_embedding: False
      external_cond_dropout: 0.0
      learn_sigma: False
      chunk_size: 256
      batch_size: 256
      depth_memory: 2
      memory_layer_indices: [1, 3, 5, 7, 9]
      momentum: False #True
    #x_shape: ${data.observation_shape}
    #external_cond_dim: ${model.external_cond_dim}

    timesteps: 1000
    beta_schedule: cosine
    schedule_fn_kwargs:
      shift: 1.0
    use_causal_mask: ${model.use_causal_mask}
    clip_noise: ${model.clip_noise}
    # training
    objective: pred_v
    loss_weighting:
      strategy: fused_min_snr
      snr_clip: 5.0
      cum_snr_decay: 0.9
    # sampling
    sampling_timesteps: ${model.sampling_timesteps}
    ddim_sampling_eta: 0.0
    # (For full sequence diffusion)
    reconstruction_guidance: 0.0

    

  #model:
  #  _target_: insert_memory.MemoryDiT3D 
  #  x_shape: ${data.observation_shape}
  #  cat_conditioning: ${model.cat_context_in_c_dim}
  #  max_tokens: ${data.max_frames}
  #  external_cond_dim: 0
  #  hidden_size: 192
  #  patch_size: 4
  #  variant: full
  #  pos_emb_type: rope_3d #learned_3d
  #  depth: 12
  #  num_heads: 4
  #  mlp_ratio: 4
  #  use_gradient_checkpointing: False
  #  use_fourier_noise_embedding: False
  #  external_cond_dropout: 0.0
  #  learn_sigma: False
  #  chunk_size: 256
  #  batch_size: 256
  #  depth_memory: 2
  #  memory_layer_indices: [1, 3, 5, 7, 9]
  #  momentum: True

    #_target_: insert_memory.DiT3D #TTT #src.models.components.dit.DiT # _S_4
    #in_channels: 6 #8
    #out_channels: 3 #4
    #input_size: 64 #16 #64
    #num_classes: 1
    #depth: 12 #12
    #hidden_size: 192 #384 #192 #384
    #num_heads: 6
    #patch_size: 4
    ## memory settings
    #batch_size: 256 #32 #64 #128 # actual splitting
    #chunk_size: 256 

  optimizer:
    _target_: torch.optim.AdamW
    _partial_: true
    lr: 0.0002 #0.0001

  lr_scheduler:
    _target_: torch.optim.lr_scheduler.LinearLR
    _partial_: true
    start_factor: 1 
    end_factor: 0.1
    total_iters: ${trainer.max_epochs}

callbacks:
  early_stopping:
    #monitor: "val/loss"
    monitor: "prediction/VideoMetricType.MSE"
    patience: 10000
    mode: "min"

  model_checkpoint:
    #monitor: "train/outer_loss"
    #monitor: "train/loss"
    #monitor: "val/loss"
    monitor: "prediction/VideoMetricType.MSE"
    mode: "min"
  
data:
  batch_size: 16 #8 #16 #32 #1 #32 #32 #64 
  num_workers: 32 #16
  n_frames: 5 #5 #25 #5
  max_frames: 2 #5 # 25 #5
  context_length: 1 #5 #25 #5
  latent_downsampling_factor: [1, 1]

logger:
  wandb:
    tags: ${tags}
    group: "ddpm_memory_maze"
  aim:
    experiment: "ddpm_memory_maze"
